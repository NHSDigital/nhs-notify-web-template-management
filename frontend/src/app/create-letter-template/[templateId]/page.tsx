'use server';

import { CreateLetterTemplate } from '@forms/CreateLetterTemplate/CreateLetterTemplate';

const CreateLetterTemplatePage = async () => <CreateLetterTemplate />;

export default CreateLetterTemplatePage;
