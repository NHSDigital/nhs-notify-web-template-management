'use server';

import type { UpdateLetterPreviewInput } from './types';

export async function updateLetterPreview(
  _: UpdateLetterPreviewInput
): Promise<void> {
  // unimplemented, date should be added to personalisation here
}
